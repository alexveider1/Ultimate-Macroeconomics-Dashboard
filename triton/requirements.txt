# Inference dependencies installed into the Triton container's Python (the
# python-backend stub interpreter). This is NOT a uv-managed service — Triton is
# NVIDIA's C++ server image, so deps are pip-installed on top of it rather than
# locked with uv. Versions are intentionally light on pins: RAPIDS cuML/cuDF
# constrain numpy/pandas tightly, so the classical-forecasting libs are left to
# resolve against whatever the GPU stack pins (freeze after a known-good build).

# --- RAPIDS GPU stack (KMeans/DBSCAN/PCA/t-SNE/UMAP); pins numpy/pandas ---
# cu13 wheels to match the CUDA 13 base image (tritonserver:25.10-vllm-python-py3).
--extra-index-url https://pypi.nvidia.com
cuml-cu13==25.10.*
cudf-cu13==25.10.*

# --- Deep learning: Amazon Chronos (Torch/T5) + GPU XGBoost ---
chronos-forecasting
xgboost

# --- Classical forecasting (CPU python backend) ---
statsmodels
pmdarima
prophet

# --- Clustering / dim-reduction CPU fallback (sklearn, umap-learn) ---
scikit-learn
umap-learn

# --- Shared helpers used by the ported inference code ---
scipy
polars
pyyaml
